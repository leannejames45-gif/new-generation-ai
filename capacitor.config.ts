import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.newgenerationai',
  appName: 'new generation ai',
  webDir: 'dist'
};

export default config;
